services:
  db:
    image: postgres:16
    container_name: asku_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: asku
      POSTGRES_USER: asku
      POSTGRES_PASSWORD: asku
    ports:
      - "5433:5432"
    volumes:
      - asku_pg:/var/lib/postgresql/data

volumes:
  asku_pg:
